/**:
  ros__parameters:
    camera:
      i_nn_type: none
      i_pipeline_type: RGBD

    rgb:
      i_board_socket_id: 0
      i_fps: 10.0              # was 15
      i_resolution: "720p"     # was 1080p — half the data

    stereo:
      i_output_disparity: false
      i_aligned_depth: true
      i_lr_check: true

    left:
      i_resolution: "400p"
      i_fps: 10.0              # was 15
      i_publish_topic: false   # disable if not needed

    right:
      i_resolution: "400p"
      i_fps: 10.0              # was 15
      i_publish_topic: false   # disable if not needed